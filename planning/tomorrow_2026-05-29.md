# 明日任务规划 — 2026-05-29

> 基于 `planning/full_planning/full_v1.md` 和当前 Stage 1 完成状态。
> 当前状态：Stage 1 全部完成（36/36 测试通过），遥操作回路闭环。

---

## 背景

Stage 1（月 1-3 硬件集成与遥操作回路）已基本完工。全规划 Stage 2 是**数据采集流水线**（月 3-6），核心目标：时间同步 <5ms、质量控制、任务设计。

## 明日任务

### 1. Docker 镜像固化（优先级：高，预计 30 分钟）

`entrypoint.sh` 的 stage_1 符号链接修复需要 `make rebuild` 才能写入镜像。目前每次 `make shell` 创建新容器时依赖 entrypoint 内置逻辑，但旧镜像里仍是错误的根目录壳体路径。

- [x] 当前：需要手动修复 symlink
- [ ] `make rebuild` 后验证 `colcon build` 能自动发现 5 个 ROS2 包（0 个手动 ln）
- [ ] 确认 `colcon build` 不加 `--symlink-install` 稳定通过
- [ ] 记录 rebuild 后的完整 `make shell cmd="...colcon build && pytest..."` 一键命令

### 2. 真实硬件验证（优先级：高，如果有硬件）

如果 xArm6 和 Quest3 都在手边：

- [ ] 启动 `teleop_real.launch.py`，确认 hand_tracking_node 收到 Quest3 UDP 数据
- [ ] 验证 hand_mapper IK 在真实数据流下的稳定性（error 分布、收敛率）
- [ ] 测试 safety_guardian 的 ESTOP 恢复流程（`/safety/reset`）
- [ ] 录制一段真实遥操作 episode（≥30 秒），检查 HDF5 数据完整性
- [ ] 如果有相机：验证 camera_node 发布 + recorder 录制图像

如果硬件不在手边，跳过此节。

### 3. 端到端延迟测量（优先级：中，预计 1 小时）

全规划对遥操作延迟的要求是 <50ms。目前尚未做过端到端延迟测量。

- [ ] 在容器内写一个脉冲测试脚本：mock_hand_tracker 发送阶跃信号 → 记录 `/teleop/command/joints` 收到时间差
- [ ] 或通过 `ros2 topic delay` 工具（如果有的话）估算各段延迟
- [ ] 记录 hand_mapper IK 求解耗时分布（`time.perf_counter` 打点）
- [ ] 输出一份延迟拆解表：tracker→mapper / mapper IK / mapper→safety / safety→robot

### 4. Stage 2 准备工作（优先级：中，预计 1 小时）

- [ ] 阅读 LeRobot 数据集规范文档（HuggingFace），对照当前 HDF5 格式找差异
- [ ] 评估当前录制的数据质量：人工查看一次 mock 录制的 episode，检查
  - 帧间关节角是否平滑（无明显跳变）
  - gripper 指令是否合理（0-1 范围内无异常值）
  - 时间戳间隔是否均匀（目标 33ms / 30Hz）
- [ ] 设计数据质量过滤规则草稿：
  - 关节速度尖峰检测（threshold: 3× velocity_limit 内的瞬态）
  - 不完整 episode 检测（< 最小步数阈值）
  - 相机帧丢失检测（间隔 > 2× 预期帧间隔）

### 5. 仿真入口调研（优先级：低，如果做完以上）

全规划建议"仿真要尽早引入"。当前 MockRobotInterface + MockHandTracker 已经是仿真雏形。

- [ ] 调研 Isaac Gym / MuJoCo 中 xArm6 模型可用性
- [ ] 确认是否只需要 RobotInterface 的一个新实现即可接入仿真环境
- [ ] 如果快速：pip install mujoco 并在容器内跑通 `mujoco.viewer`（验证显示转发）

---

## 建议优先级

| 优先级 | 任务 | 理由 |
|--------|------|------|
| 1 | 镜像固化 | 每次创建容器手动修 symlink 已重复多次，该修根了 |
| 2 | 硬件验证 | 如果有硬件：Stage 1 闭环的真正验证，早测早发现问题 |
| 3 | 延迟测量 | Stage 2 <50ms 目标的基础数据，不测不知道现状 |
| 4 | 数据质量 | Stage 2 准备工作，越早越好 |
| 5 | 仿真调研 | 长期价值高，但不阻塞 |

如果时间有限，只做 1 + 2（如果有硬件）或 1 + 3（如果无硬件）。
