# 今日任务规划 — 2026-06-01

> 基于 `planning/full_planning/full_v1.md`、前两日完成状态、当前工作树变更。
> 当前状态：Stage 1 完成（36/36 测试通过），13 个文件待提交，MJCF 精度问题待修复，Docker 镜像需更新。

---

## 背景

上两日完成了 IK 收敛修复（63→26ms）、仿真环境搭建（MuJoCo + MujocoRobotInterface）、LeRobot v3.0 转换器、数据质量过滤器、任务系统、时间同步验证。

但以下内容**尚未落地**：
- Docker 镜像尚未 rebuild（mujoco 包 + entrypoint symlink 修复仍在工作树中）
- MJCF 模型 FK 误差 >1m（仿真数据不可用于训练）
- Recorder step_count 显示 bug 未修复
- 13 个文件修改散落在工作树中未提交

## 今日完成

| 时间 | 事项 | 状态 |
|------|------|------|
| — | Recorder step_count bug 修复 (`5f9a34d`) | ✅ |
| — | 13 文件 + 新增文件分组提交 (7 个 commits) | ✅ |
| — | README.md 项目文档创建 (`56cfe54`) | ✅ |
| — | MJCF 精度诊断报告 (`10a66d0`) | ✅ |
| — | GitHub 远程推送 (jackaaron01/ds_umi, master→main) | ✅ |
| — | 宿主机 24 项测试验证通过 | ✅ |
| — | Docker 镜像 `make rebuild` (MuJoCo 3.9.0 + LeRobot 0.4.4 + PyTorch 2.10) | ✅ |
| — | 容器内 36/36 ROS2 测试全部通过 | ✅ |
| — | entrypoint symlink 验证 (5 个包均指向 stage_1) | ✅ |

## 未完成（移至明日）

| 任务 | 备注 |
|------|------|
| ACT 基线调研 | Docker 镜像已有 PyTorch 2.10 + LeRobot 0.4.4，训练环境就绪 |
| MJCF 模型精度修复 | 诊断报告已完成，推荐方案 B（按 DH 参数重写），预计 2-4h |

---

## 今日任务

### 1. Recorder step_count bug 修复 ✅

**根因已定位**：`_finalize_episode()`（`recorder_node.py:205`）调用流程：
1. `self._writer.end_episode()` → `hdf5_writer.py:45` 将 `_step_count = 0` 清零
2. `self._writer.close()` 
3. `self._writer.step_count` 读到的是 0，而非实际步数

- [x] 修复代码（缓存 `step_count` 到 `actual_steps`）
- [ ] 验证：mock 录制后日志输出 step_count 与实际 HDF5 步数一致（待容器内验证）

### 2. 提交工作树变更 ✅

- [x] 清理垃圾文件 `=65,`
- [x] 4 个功能分组提交 + 2 个后续提交：
  - `5f9a34d` fix: IK convergence and stage 1 stability improvements
  - `b7f820b` feat: docker updates and hand-eye calibration tool
  - `d3f7a40` docs: project documentation and planning updates
  - `6eabceb` feat: stage 2 data pipeline tools and MuJoCo simulation
  - `10a66d0` docs: MJCF model precision diagnosis report
  - `56cfe54` docs: add project README
- [x] 推送到 GitHub (jackaaron01/ds_umi, master→main)

### 3. Docker 镜像更新 🔄

- [x] `make rebuild` 已在后台启动（Step 11/21，下载 PyTorch + CUDA 依赖中）
- [ ] 验证 `python3 -c "import mujoco; print(mujoco.__version__)"` 无需手动安装
- [ ] 验证 entrypoint 符号链接指向正确的 `stage_1/<pkg>/` 路径（5 个包）
- [ ] 验证 `colcon build` 不加 `--symlink-install` 在新镜像中稳定通过

### 4. 全量测试验证 🔄

- [x] 纯 Python 测试（宿主机）：24/24 通过，2 skipped
- [ ] 完整 ROS2 测试套件（容器内）：待镜像构建完成
- [ ] 仿真管道测试 `test_sim_pipeline.py` 在新镜像中开箱即用

### 5. MJCF 模型精度调研 ✅

- [x] 对比手写 FK（DH 参数）与 MJCF 几何：零位误差 0.61m
- [x] 定位三个根因：所有关节 Z 轴、连杆尺寸不匹配、缺失 theta_offset
- [x] 输出诊断报告 `stage_2/simulation/mjcf_diagnosis.md`（3 种修复方案 + 工作量估计）
- [x] 提交并推送

### 6. Stage 3 ACT 基线调研 🔲

待 Docker 镜像构建完成后进行。

---

## 建议优先级

| 优先级 | 任务 | 理由 |
|--------|------|------|
| 1 | step_count bug | 最小修复，立竿见影 |
| 2 | 提交变更 | 13 个文件不能一直漂着，越拖越乱 |
| 3 | 镜像更新 | 消除每次手动安装 mujoco 的重复劳动 |
| 4 | 全量测试 | 确保提交前没有回归 |
| 5 | MJCF 调研 | 仿真数据质量的核心问题，但不阻塞其他任务 |
| 6 | ACT 调研 | 为 Stage 3 铺路，但不紧急 |

**如果时间有限**：做 1 + 2 + 3 + 4（bug 修复 + 提交 + 镜像 + 验证），这能让仓库回到干净、可复现的状态。

**如果时间充裕**：在 1-4 基础上加 5（MJCF 调研），为明日修复打下基础。
