# 明日任务规划 — 2026-06-02

> 基于 `planning/full_planning/full_v1.md` 和今日完成状态。
> 当前状态：Stage 1 完成（36/36 测试），MJCF FK 精度已修复（0mm），ACT 数据管道兼容性已验证。

---

## 背景

今日完成了 Docker 镜像更新、MJCF 模型精度修复（>1m→0mm）、CLAUDE.md 更新。ACT 调研进行了一半——数据管道已验证兼容，但验证脚本有 bug 待修，训练方案文档待写。

Docker 镜像已包含所有训练依赖：PyTorch 2.10 + CUDA 12.9 + LeRobot 0.4.4 + MuJoCo 3.9.0。

---

## 明日任务

### 1. ACT 验证脚本修复 + 训练方案文档（优先级：高，预计 1 小时）

今日 `stage_2/act_research.py` 已能生成录制并调用 `convert_directory()`，但验证阶段有导入路径问题。

- [ ] 修复 `verify_v3()` 中的 info.json 读取路径
- [ ] 端到端跑通：mock 录制 → v3 转换 → 格式验证
- [ ] 输出 ACT 训练方案文档（`stage_2/act_training_plan.md`），包含：
  - LeRobot v3.0 数据格式要点
  - 从录制到训练的完整命令
  - 建议的超参数（chunk_size=100, n_action_steps=100）
  - GPU 资源需求预估

### 2. 多 episode 数据生成（优先级：高，预计 1-2 小时）

ACT 训练需要 50+ episodes。当前只有单 episode 录制。

- [ ] 写批量 mock 录制脚本：生成 50–100 个 mock episodes
- [ ] 全量转换为 LeRobot v3.0 格式
- [ ] 验证 LeRobot 能加载多 episode 数据集
- [ ] 检查 stats.json 归一化统计量是否合理

### 3. ACT 小规模训练验证（优先级：中，预计 2-3 小时，需 GPU）

在 mock 数据上跑通完整训练回路。

- [ ] 用 mock 数据进行小规模 ACT 训练（10000 steps, batch_size=8）
- [ ] 验证训练 loss 正常下降
- [ ] 验证 checkpoint 保存和加载
- [ ] 记录容器内训练命令和注意事项

### 4. 数据质量过滤器文档化（优先级：低，预计 30 分钟）

`stage_2/quality_filters.py` 已实现 9 项检查但缺少使用文档。

- [ ] 为 `analyze_episode()` 写 docstring 和示例
- [ ] 确认与 v3.0 转换管道的集成点

### 5. 清理临时脚本（优先级：低，预计 15 分钟）

- [ ] 评估 `stage_2/act_research.py`、`act_check.sh` 是否应保留
- [ ] 移除纯调试用途的脚本，或整合到正式工具中

---

## 建议优先级

| 优先级 | 任务 | 理由 |
|--------|------|------|
| 1 | ACT 验证脚本修复 | 完成今日未竟事项 |
| 2 | 多 episode 数据生成 | ACT 训练的前置条件 |
| 3 | ACT 小规模训练 | Stage 3 的首次实战验证 |
| 4 | 质量过滤器文档 | 提升 Stage 2 工具可用性 |
| 5 | 清理临时脚本 | 保持仓库整洁 |

**如果时间有限**：做 1 + 2（ACT 验证 + 数据生成），完成数据管道闭环。
**如果有 GPU**：优先做 3（ACT 训练），这是最有价值的里程碑。
